import { Paper } from '@mui/material';
import styled from 'styled-components';

export const ProductInCartParamWrapper = styled(Paper)`
  display: flex;
  align-items: center;
  flex-direction: row;
  padding: 0 4px;
  background-color: #f0f0f0 !important;
`;
export const CartPopoverImageWrapper = styled.div`
  cursor: pointer;
`;
